#!/bin/bash
# Wrapper script for HestAI MCP Server CLI compatibility

# Get the directory of this script
DIR="$(cd "$(dirname "$0")" && pwd)"

# Change to the hestai-mcp-server directory
cd "$DIR"

# Execute the Python server with all arguments passed through
exec .hestai_venv/bin/python server.py "$@"