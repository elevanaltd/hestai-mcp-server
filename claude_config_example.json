{
  "comment": "Example Claude Desktop configuration for HestAI MCP Server",
  "comment2": "Run './run-server.sh -c' to get the exact configuration for your system",
  "comment3": "For platform-specific examples, see the examples/ directory",
  "mcpServers": {
    "hestai": {
      "command": "/path/to/hestai-mcp-server/.hestai_venv/bin/python",
      "args": ["/path/to/hestai-mcp-server/server.py"]
    }
  }
}